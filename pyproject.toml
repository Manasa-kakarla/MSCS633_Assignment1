[project]
name = "biox_qr_generator"
version = "1.0.0"
description = "A QR code generator for Biox Systems"
authors = [
    { name="Manasa", email="vekakarla27517@ucumberlands.edu" }
]
requires-python = ">=3.7"
dependencies = [
    "qrcode[pil]"
]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
